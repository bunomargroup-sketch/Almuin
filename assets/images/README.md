# Images

Almuin's design is typographic-first (glass surfaces + Islamic star motif),
so almost no raster imagery is required. Drop optional launch/hero artwork
here if desired and reference it from `pubspec.yaml`.
